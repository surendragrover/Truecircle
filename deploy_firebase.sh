#!/bin/bash

# This deployment script was sanitized for the offline-only build.
# All Firebase/App Distribution steps were removed to eliminate
# traces of Google/Firebase from the repository. To deploy builds,
# use your private CI or a separate deployment tool outside this repo.

echo "This repo is configured as offline-only; Firebase deployment steps have been removed."